box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.15"
        theme: angels-ladder
        flags: --buildDrafts=true
deploy:
  steps:
    - install-packages:
        packages: perl liberror-perl git ssh-client
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: blog.mursts.jp
        basedir: public
