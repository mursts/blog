box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: angels-ladder
        flags: --buildDrafts=true
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: blog.mursts.jp
        basedir: public
