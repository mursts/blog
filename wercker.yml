box: mursts/hugo-deploy
build:
  steps:
    - arjen/hugo-build:
        theme: hugo-future-imperfect
        flags: --buildDrafts=false
deploy:
  steps:
    - keima/go-appengine-util:
        method: deploy
        gopath: $WERCKER_SOURCE_DIR
        token: $APP_ENGINE_TOKEN
